Объявление многомерного массива выполняется так же просто, как и объявление одномерного массива, с той лишь разницей, что теперь для массива указывается размер по каждому из индексов. Для каждого индекса используется собственная пара квадратных скобок.

Среди многомерных массивов самым простым является двумерный массив. Двумерный массив - это массив из одномерных массивов. Например, `double n[4][5]` объявление двумерного массива массива действительных числе двойной точности размером 4 на 5.

Рассмотрим использование двумерных массивов на примере сложения двух матриц. У нас будет три матрицы: `a[100][100]`, `b[100][100]` и `sum[100][100]`. Последняя будет содержать сумму матриц a и b.

![[Pasted image 20240418140831.png]]
![[Pasted image 20240418140842.png]]
![[Pasted image 20240418140847.png]]

Рассмотрим другой пример работы с многомерными массивами - умножение двух матриц. Чтобы умножить две матрицы, число колонок первой матрицы должно быть равно количеству строк второй матрицы. Программа отобразит ошибку если это не так.

Общая формула произведения матрицы выглядит так:
![[Pasted image 20240418143759.png]]

![[Pasted image 20240418143852.png]]

На рисунке видно, что были созданы две матрицы размером 2 на 2. Все элементы первой матрицы равны 2, второй - 3. В результате мы получим матрицу размером 2 на 2, все элементы которой равны 12. Компоненты результирующей матрицы вычисляются следующим образом:
![[Pasted image 20240418144022.png]]

Вот код программы:
![[Pasted image 20240418144048.png]]
![[Pasted image 20240418144103.png]]
![[Pasted image 20240418144119.png]]

Другой пример, вычисление транспонированной матрицы, а затем ее вывод. ==Транспонированная матрица== - матрица, полученная из исходной матрицы заменой строк на столбцы.

Код:
![[Pasted image 20240418150159.png]]
![[Pasted image 20240418150209.png]]
![[Pasted image 20240418150219.png]]
![[Pasted image 20240418150230.png]]
